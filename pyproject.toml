# pyproject.toml

[tool.pytest.ini_options]
# Pythonの検索パスにプロジェクトのルートディレクトリ（カレントディレクトリ）を追加する
# これにより、テストファイルから 'mnist' をインポートできるようになる
pythonpath = ["."]

# pytestがテストを検索するディレクトリを指定することもできます
# testpaths = ["tests"]